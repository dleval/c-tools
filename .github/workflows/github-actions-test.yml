name: GitHub Actions Test
on: [workflow_dispatch]
jobs:
  testunit:
    image: throwtheswitch/madsciencelab:0.3.1
    steps:
      - run: echo "Run desktop (native) unit tests"
      - run: ceedling test:all